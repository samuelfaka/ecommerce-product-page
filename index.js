function dropup(){
    const pop = document.querySelector(".puma")
    pop.style.display = "flex";
    pop.style.scale = "1.1"
       const cancel = document.querySelector(".cancel")
    cancel.style.display = "flex"
}

function cancel(){
     pop.style.display = "flex";
    pop.style.scale = "1.1"
      pop.cancel = "flex"
}